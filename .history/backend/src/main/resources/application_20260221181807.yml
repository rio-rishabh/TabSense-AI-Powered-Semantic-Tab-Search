server:
  port: 8080

gemini:
  api-key: ${GEMINI_API_KEY:}
  model-name: "gemini-1.5-flash"

app:
  rag:
    chunk-size: 500
    top-k: 5
  scraper:
    page-timeout: 30000
    max-concurrent-pages: 5